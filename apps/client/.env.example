# Usado somente pelo proxy local do Vite; não é enviado para o navegador.
API_PROXY_TARGET=http://localhost:3000

# Runtime de produção Bun (bun run start), separado do proxy do Vite:
# API_HOST=localhost
# API_PORT=3000
# PORT=3001
# PUBLIC_URL=http://localhost:3001
# LOG_LEVEL=info
